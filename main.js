var loadgame = function(){
    var Name = prompt("Now that the game's loaded, welcome to GAIA! What's your name?");
    var Answer = prompt("Alright, " + Name + "! What's 7 + 7?");
    if (Answer === "14"){
        
    }
};
var points = "undefined";
alert("Press OK to prompt game. This requires a tiny bit of waiting.");
setTimeout(loadgame, 3000);




